
*.log
/tmp/pyinstaller_build/
# Python - ignore the venv and pyinstaller temp files, but KEEP python/dist/solver (needed for go:embed)
build/bin
build/bin/
frontend/dist
goGearset
node_modules
python/__pycache__/
python/.venv/
python/*.pyc
# solver.spec is checked in on purpose — build-windows.ps1/build_releases.sh
# both need it (pyinstaller --noconfirm solver.spec) and it must exist for a
# fresh clone to build at all, same reasoning as keeping python/dist/solver.
python/build/
python/dist/
# Fetched by ensureDDOBuilderData() (app.go) on first run — data, not source.
/DDOBuilderV2/
/DDOBuilderV2.download/
/.ddobuilderv2_commit
*.ddogearset
gearset_output.txt

test-pulp.lp

test_glpk.py

python/tests/__pycache__/

# Every __pycache__, wherever it appears — the per-directory rules above
# missed python/rules/ when that package was added.
__pycache__/

# Scratch dir used by scripts/capture_oracle.py while replaying gearsets
# through the solver. The captured fixtures themselves
# (python/tests/fixtures/oracle/*.oracle.json) ARE tracked on purpose — they are
# the differential oracle for the 0.5.0 recalculation work and cannot be
# regenerated once mode:"calculate" is removed. See
# docs/0.5.0/01_RECALC_SPEC_AND_PHASED_PLAN.md Phase 0.
python/tests/fixtures/.capture_tmp/

frontend/wailsjs/runtime/package.json

frontend/wailsjs/runtime/runtime.d.ts

frontend/wailsjs/runtime/runtime.js

.DS_Store

dist/darwin-arm64/DDO Gearset Optimizer.zip

# Full canonical parser output written by scripts/parser_snapshot.py.
# Only the digests file is tracked; this is the local diffing aid.
python/tests/fixtures/.snapshot_full/
