# Handoff & Next Steps (Timestamp: 2026-08-04 08:16 CST)

## 1. Data Analysis (Caster Keywords)
- **Goal:** Analyze the `DDOBuilder` XML data to identify the exact keywords and node structures that represent caster-specific stats.
- **Specific Stats to Find:** 
  - Spell Critical Chance (e.g., "Fire Lore", "Kinetic Lore", or explicit critical chance tags).
  - Spell Critical Damage (e.g., "Spell Critical Damage").
  - Spellpower Values (e.g., "Combustion", "Magnetism", "Devotion", or explicit spellpower tags).
  - Spell School DCs (e.g., "Evocation Focus", "Necromancy Focus").
- **Action:** Before implementing the logic, run a script or search through `Items.xml` and `Augments.xml` to build a mapping of how these stats are officially named in the XML, so they can be reliably parsed and prioritized.

## 2. UI Updates: Caster Multi-Selectors
- **Goal:** Dynamically show caster-specific options when the user selects "Caster" as their Build Type.
- **Requirements:** 
  - Add a multi-selector for **Spell Schools** (e.g., Evocation, Necromancy, Enchantment).
  - Add a multi-selector for **Spellpowers** (e.g., Fire, Force, Positive, Negative).
  - When the user selects items in these multi-selectors, automatically inject them into their **Stat Priorities** list.

## 3. UI Updates: Stat Priority Reordering
- **Goal:** Allow the user to drag-and-drop or use up/down arrows to reorder their Stat Priorities list.
- **Context:** Order currently dictates priority weights mathematically (e.g. geometric decay for multiple 100-weight stats, or filigree bias). Being able to reorder them is critical for fine-tuning the solver's focus.

## 4. UI Updates: Form Layout Refinements
- **Goal:** Clean up the `JobConfigurationForm.svelte` layout for better UX.
- **Requirements:**
  - Move the **Max Search Time** field to the very bottom of the configuration form.
  - Group the following three artifact-related fields together (potentially in a distinct panel or tight cluster):
    1. **Reserved Minor Artifact Slot** (Dropdown)
    2. **Minor Artifact Filigree Slots** (Numeric only, display as a small/compact column)
    3. **Dinosaur Bone Artifact** (Checkbox)
